name: pierre
description: Non intrusive and open-source gallery app.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=2.19.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  photo_view: ^0.15.0
  video_player: ^2.9.2
  chewie: ^1.9.2
  media_store_plus: ^0.1.3
  permission_handler: ^11.3.1
  shared_preferences: ^2.4.0
  external_path: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
